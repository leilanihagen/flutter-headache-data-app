import 'package:flutter/material.dart';

class YourDataScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Your Data - Headache Data'),
      ),
      body: Center(
        child: Column(
          
        ),
      ),
    );
  }
}